interface:
  display_name: "Sem Compile"
  short_description: "Compile requests into inspectable Sem programs"
  default_prompt: "Use $sem-compile to turn this request or sketch into a Sem program without running it."
policy:
  allow_implicit_invocation: false
