interface:
  display_name: "N Whys"
  short_description: "Trace a why-chain to an explicit depth"
  default_prompt: "Use $n-whys with an explicit depth to trace this question through successive explanations."
policy:
  allow_implicit_invocation: false
